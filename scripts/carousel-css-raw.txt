/* Hero promo carousel â€” display toggled in min/max-width media queries */
.hero-promo-carousel__viewport {
  width: 100%;
  overflow: hidden;
}

.hero-promo-carousel__track {
  display: flex;
  width: 100%;
  will-change: transform;
}

.hero-promo-carousel--js .hero-promo-carousel__track {
  touch-action: pan-y pinch-zoom;
}

.hero-promo-carousel--js .hero-promo-carousel__viewport {
  touch-action: pan-y;
}

.hero-promo-carousel__slide {
  position: relative;
  display: flex;
  flex: 0 0 100%;
  min-width: 100%;
  min-height: clamp(400px, 60vh, 520px);
  border-radius: 1.65rem;
  overflow: hidden;
}

.hero-promo-card {
  position: relative;
  display: flex;
  width: 100%;
  height: 100%;
  overflow: hidden;
  border-radius: inherit;
  border: 1px solid rgba(255, 255, 255, 0.72);
  box-shadow:
    0 32px 72px -36px rgba(44, 36, 32, 0.58),
    0 0 0 1px rgba(184, 150, 110, 0.1),
    inset 0 1px 0 rgba(255, 255, 255, 0.65);
}

.hero-promo-card__bg {
  position: absolute;
  inset: 0;
  z-index: 0;
}

.hero-promo-card--ocean .hero-promo-card__bg {
  background:
    radial-gradient(ellipse 90% 70% at 50% 0%, rgba(168, 198, 218, 0.45) 0%, transparent 58%),
    linear-gradient(165deg, #f5f0ea 0%, #e8eef3 42%, #f0ebe4 100%);
}

.hero-promo-card--sand .hero-promo-card__bg {
  background:
    radial-gradient(ellipse 80% 60% at 80% 20%, rgba(212, 175, 120, 0.28) 0%, transparent 55%),
    linear-gradient(155deg, #faf6f0 0%, #f3ebe0 48%, #efe6d8 100%);
}

.hero-promo-card--coral .hero-promo-card__bg {
  background:
    radial-gradient(ellipse 75% 65% at 20% 15%, rgba(224, 160, 140, 0.32) 0%, transparent 52%),
    linear-gradient(160deg, #faf4f1 0%, #f5e8e4 45%, #f0ebe6 100%);
}

.hero-promo-card__orb {
  position: absolute;
  z-index: 0;
  border-radius: 50%;
  filter: blur(40px);
  pointer-events: none;
}

.hero-promo-card__orb--1 {
  top: -18%;
  right: -12%;
  width: 55%;
  aspect-ratio: 1;
  background: rgba(184, 150, 110, 0.18);
}

.hero-promo-card__orb--2 {
  bottom: -22%;
  left: -10%;
  width: 48%;
  aspect-ratio: 1;
  background: rgba(120, 160, 180, 0.14);
}

.hero-promo-card__grain {
  position: absolute;
  inset: 0;
  z-index: 0;
  opacity: 0.35;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.5'/%3E%3C/svg%3E");
  mix-blend-mode: soft-light;
  pointer-events: none;
}

.hero-promo-card__inner {
  position: relative;
  z-index: 1;
  display: flex;
  width: 100%;
  height: 100%;
  flex-direction: column;
  padding: 1rem 1.15rem 1.35rem;
}

.hero-promo-card__brand {
  margin-bottom: 0.55rem;
}

.hero-promo-card__layout {
  display: flex;
  flex: 1;
  flex-direction: column;
  gap: 0.85rem;
}

.hero-promo-card__stage {
  position: relative;
  display: flex;
  flex: 1;
  align-items: center;
  justify-content: center;
  min-height: 12.5rem;
}

.hero-promo-card__ring {
  position: absolute;
  top: 50%;
  left: 50%;
  width: min(92%, 280px);
  aspect-ratio: 1;
  border: 1px solid rgba(184, 150, 110, 0.22);
  border-radius: 50%;
  transform: translate(-50%, -50%);
  animation: hero-promo-ring-spin 28s linear infinite;
  pointer-events: none;
}

.hero-promo-card__ring--inner {
  width: min(78%, 230px);
  border-color: rgba(184, 150, 110, 0.14);
  animation-direction: reverse;
  animation-duration: 20s;
}

@keyframes hero-promo-ring-spin {
  to {
    transform: translate(-50%, -50%) rotate(360deg);
  }
}

.hero-promo-card__frame {
  position: relative;
  z-index: 2;
  width: min(72%, 220px);
  aspect-ratio: 4 / 5;
  overflow: hidden;
  border-radius: 58% 42% 52% 48% / 42% 42% 58% 58%;
  background: linear-gradient(155deg, rgba(255, 255, 255, 0.95) 0%, rgba(248, 244, 239, 0.8) 100%);
  box-shadow:
    0 28px 56px -22px rgba(44, 36, 32, 0.42),
    inset 0 0 0 1px rgba(255, 255, 255, 0.85),
    0 0 0 1px rgba(184, 150, 110, 0.14);
}

.hero-promo-card__photo {
  object-fit: cover;
  object-position: center;
}

.hero-promo-card__float {
  position: absolute;
  z-index: 3;
  left: 4%;
  bottom: 8%;
  min-width: 148px;
  padding: 0.85rem 1rem;
  border: 1px solid rgba(184, 150, 110, 0.18);
  border-radius: var(--radius-md);
  background: rgba(248, 244, 239, 0.9);
  backdrop-filter: blur(12px);
  box-shadow: var(--shadow-card);
  animation: fadeUp 0.75s 0.15s ease both;
}

.hero-promo-card__float-eyebrow {
  display: block;
  margin-bottom: 0.3rem;
  font-family: var(--font-sans);
  font-size: 0.56rem;
  font-weight: 600;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--gold);
}

.hero-promo-card__float-title {
  display: block;
  font-family: var(--font-serif);
  font-size: 0.92rem;
  line-height: 1.25;
  color: var(--charcoal);
}

.hero-promo-card__float-price {
  display: block;
  margin-top: 0.2rem;
  font-family: var(--font-sans);
  font-size: 0.78rem;
  font-weight: 500;
  color: var(--taupe);
}

.hero-promo-card__seal {
  position: absolute;
  top: 2%;
  right: 6%;
  z-index: 3;
  display: flex;
  width: 68px;
  height: 68px;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: linear-gradient(145deg, var(--gold) 0%, #c9a87c 55%, var(--gold-dark) 100%);
  box-shadow: var(--shadow-gold);
  animation: fadeIn 0.9s 0.25s ease both;
}

.hero-promo-card__seal-main {
  font-family: var(--font-serif);
  font-size: 0.82rem;
  line-height: 1;
  text-align: center;
  color: var(--white);
  letter-spacing: 0.04em;
}

.hero-promo-card__copy {
  position: relative;
  display: flex;
  width: 100%;
  flex-direction: column;
  align-items: center;
  gap: 0.4rem;
  padding: 0.85rem 0.75rem 0.25rem;
  border-radius: 1.15rem;
  background: rgba(255, 255, 255, 0.55);
  backdrop-filter: blur(14px);
  border: 1px solid rgba(255, 255, 255, 0.75);
  text-align: center;
}

.hero-promo-card__index {
  position: absolute;
  top: -0.15rem;
  right: 0.35rem;
  font-family: var(--font-serif);
  font-size: clamp(2.75rem, 12vw, 3.5rem);
  font-weight: 300;
  line-height: 1;
  color: rgba(184, 150, 110, 0.14);
  pointer-events: none;
  user-select: none;
}

.hero-promo-card__category {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.55rem;
  margin: 0;
  font-family: var(--font-sans);
  font-size: 0.58rem;
  font-weight: 600;
  letter-spacing: 0.24em;
  text-transform: uppercase;
  color: var(--gold);
}

.hero-promo-card__category-line {
  display: inline-block;
  width: 22px;
  height: 1px;
  background: var(--gold);
}

.hero-promo-card__name {
  margin: 0;
  font-family: var(--font-serif);
  font-size: clamp(1.35rem, 4.8vw, 1.65rem);
  font-weight: 400;
  line-height: 1.15;
  letter-spacing: -0.01em;
  color: var(--charcoal);
}

.hero-promo-card__detail {
  margin: 0;
  font-family: var(--font-sans);
  font-size: 0.68rem;
  font-weight: 300;
  line-height: 1.5;
  color: var(--taupe);
}

.hero-promo-card__meta {
  display: flex;
  width: 100%;
  align-items: center;
  justify-content: center;
  gap: 1rem;
  margin-top: 0.15rem;
}

.hero-promo-card__price {
  font-family: var(--font-serif);
  font-size: 1.35rem;
  font-weight: 500;
  color: var(--charcoal);
}

.hero-promo-card__rating {
  display: inline-flex;
  align-items: center;
  gap: 0.3rem;
}

.hero-promo-card__stars {
  font-size: 0.55rem;
  letter-spacing: 0.05em;
  color: var(--gold);
}

.hero-promo-card__rating-num {
  font-family: var(--font-sans);
  font-size: 0.62rem;
  font-weight: 600;
  color: var(--taupe);
}

.hero-promo-card__cta {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.45rem;
  width: 100%;
  margin-top: 0.65rem;
  padding: 0.82rem 1.4rem;
  border-radius: 999px;
  background: linear-gradient(135deg, #2c2420 0%, #4a3d32 52%, #5c4a3a 100%);
  color: #fff;
  font-family: var(--font-sans);
  font-size: 0.62rem;
  font-weight: 600;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  text-decoration: none;
  box-shadow: 0 14px 32px -16px rgba(44, 36, 32, 0.6);
  touch-action: manipulation;
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.hero-promo-card__cta:hover {
  transform: translateY(-1px);
  box-shadow: 0 18px 36px -14px rgba(44, 36, 32, 0.65);
}

.hero-promo-card__cta:active {
  transform: scale(0.98);
}

.hero-promo-carousel__footer {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 1rem;
  margin-top: 0.75rem;
  padding: 0.1rem 0;
}

.hero-promo-carousel__counter {
  display: none;
  align-items: baseline;
  gap: 0.2rem;
  font-family: var(--font-sans);
  font-size: 0.62rem;
  font-weight: 500;
  letter-spacing: 0.14em;
  color: var(--taupe);
}

.hero-promo-carousel__counter-current {
  font-family: var(--font-serif);
  font-size: 1.15rem;
  font-weight: 400;
  color: var(--charcoal);
}

.hero-promo-carousel__counter-sep {
  opacity: 0.45;
}

.hero-promo-carousel__dots {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.15rem;
  padding: 0.15rem 0;
}

.hero-promo-nav-radio {
  position: absolute;
  width: 1px;
  height: 1px;
  margin: 0;
  padding: 0;
  opacity: 0;
  pointer-events: none;
}

label.hero-promo-carousel__dot {
  position: relative;
  z-index: 2;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0;
  padding: 0.55rem 0.35rem;
  cursor: pointer;
  touch-action: manipulation;
  -webkit-tap-highlight-color: transparent;
  pointer-events: auto;
}

.hero-promo-carousel__dot-track {
  position: relative;
  display: block;
  width: 6px;
  height: 6px;
  overflow: hidden;
  border-radius: 999px;
  background: rgba(184, 150, 110, 0.35);
  transition:
    width 0.35s cubic-bezier(0.22, 1, 0.36, 1),
    background 0.3s ease;
}

.hero-promo-nav-radio:checked + label.hero-promo-carousel__dot .hero-promo-carousel__dot-track {
  width: 1.65rem;
  background: rgba(184, 150, 110, 0.22);
}

.hero-promo-carousel__dot-progress {
  position: absolute;
  top: 0;
  left: 0;
  height: 100%;
  width: 0;
  border-radius: inherit;
  background: linear-gradient(90deg, var(--gold) 0%, var(--gold-light) 100%);
  animation: none;
}

.hero-promo-nav-radio:checked + label.hero-promo-carousel__dot .hero-promo-carousel__dot-progress {
  animation: hero-promo-dot-progress 5.5s linear forwards;
}

@keyframes hero-promo-dot-progress {
  from {
    width: 0%;
  }

  to {
    width: 100%;
  }
}

.hero-promo-carousel__slide {
  transition: opacity 0.45s ease;
}

.hero-promo-carousel__slide:not(.is-active) .hero-promo-card__copy {
  opacity: 0.92;
}

.hero-promo-carousel__slide.is-active .hero-promo-card__copy {
  animation: hero-promo-copy-in 0.55s cubic-bezier(0.22, 1, 0.36, 1) both;
}

.hero-promo-carousel__slide.is-active .hero-promo-card__frame {
  animation: hero-promo-product-in 0.65s cubic-bezier(0.22, 1, 0.36, 1) both;
}

.hero-promo-carousel__slide.is-active .hero-promo-card__float {
  animation: hero-promo-copy-in 0.6s 0.08s cubic-bezier(0.22, 1, 0.36, 1) both;
}

.hero-promo-carousel__slide.is-active .hero-promo-card__seal {
  animation: hero-promo-product-in 0.7s 0.12s cubic-bezier(0.22, 1, 0.36, 1) both;
}

@keyframes hero-promo-product-in {
  from {
    opacity: 0;
    transform: translateY(14px) scale(0.94);
  }

  to {
    opacity: 1;
    transform: translateY(-2px) scale(1);
  }
}

@keyframes hero-promo-copy-in {
  from {
    opacity: 0;
    transform: translateY(10px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@media (max-width: 900px) {
  .hero-section {
    align-items: flex-start;
    min-height: 0;
    padding-top: calc(max(14px, env(safe-area-inset-top, 0px)) + 52px + 0.85rem);
    padding-bottom: 1.75rem;
    background: var(--sand-light);
    overflow: visible;
  }

  .hero-section .container {
    width: 100%;
    max-width: none;
    padding-inline: 1rem;
  }

  .hero-section__bg {
    display: none;
  }

  .hero-visual__desktop {
    display: none;
  }

  .hero-promo-carousel {
    display: block;
    width: 100%;
  }

  .hero-promo-carousel__nav {
    display: none;
  }

  .hero-visual {
    position: relative;
    z-index: 1;
    display: block;
    width: 100%;
    min-height: 0;
    padding: 0;
  }

  .hero-section__photo {
    object-position: center 42%;
    transform: none;
  }

  .hero-section__shade {
    background:
      linear-gradient(
        180deg,
        rgba(248, 244, 239, 0.08) 0%,
        transparent 28%,
        rgba(248, 244, 239, 0.18) 72%,
        rgba(248, 244, 239, 0.72) 100%
      );
  }

  .hero-section__vignette {
    box-shadow: none;
  }

  .hero-grid {
    display: flex;
    flex-direction: column;
    width: 100%;
    gap: 0;
  }

  .hero-carousel-area {
    display: block;
    order: -1;
    width: 100%;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    gap: 0;
    width: 100%;
    padding: 0;
    animation: none;
  }

  .hero-promo-carousel .hero-promo-card__brand {
    display: flex;
    width: 100%;
    flex-direction: column;
    align-items: center;
    gap: 0.45rem;
  }

  .hero-promo-card__inner {
    padding: 0.85rem 1rem 1.15rem;
    text-align: center;
  }

  .hero-promo-card__copy {
    background: rgba(255, 255, 255, 0.72);
    border-top: none;
    border-radius: 0 0 1.15rem 1.15rem;
    padding-top: calc(0.85rem + 16px);
    
    --card-wave-h: 16px;
    -webkit-mask-image:
      url("data:image/svg+xml,%3Csvg viewBox='0 0 1440 56' xmlns='http://www.w3.org/2000/svg' preserveAspectRatio='none'%3E%3Cpath fill='%23fff' d='M0 28C180 8 360 48 540 28S900 8 1080 28 1260 48 1440 28V56H0Z'/%3E%3C/svg%3E"),
      linear-gradient(#fff, #fff);
    mask-image:
      url("data:image/svg+xml,%3Csvg viewBox='0 0 1440 56' xmlns='http://www.w3.org/2000/svg' preserveAspectRatio='none'%3E%3Cpath fill='%23fff' d='M0 28C180 8 360 48 540 28S900 8 1080 28 1260 48 1440 28V56H0Z'/%3E%3C/svg%3E"),
      linear-gradient(#fff, #fff);
    -webkit-mask-size: 600px var(--card-wave-h), 100% calc(100% - var(--card-wave-h));
    mask-size: 600px var(--card-wave-h), 100% calc(100% - var(--card-wave-h));
    -webkit-mask-position: 0 0, 0 var(--card-wave-h);
    mask-position: 0 0, 0 var(--card-wave-h);
    -webkit-mask-repeat: repeat-x, no-repeat;
    mask-repeat: repeat-x, no-repeat;
    animation: promo-wave-drift 12s linear infinite;
  }

  @keyframes promo-wave-drift {
    0% {
      -webkit-mask-position: 0 0, 0 var(--card-wave-h);
      mask-position: 0 0, 0 var(--card-wave-h);
    }
    100% {
      -webkit-mask-position: -600px 0, 0 var(--card-wave-h);
      mask-position: -600px 0, 0 var(--card-wave-h);
    }
  }

  .hero-promo-card__float {
    left: 6%;
    bottom: 6%;
    min-width: 132px;
    padding: 0.7rem 0.85rem;
  }

  .hero-promo-card__seal {
    width: 58px;
    height: 58px;
    top: 0;
    right: 4%;
  }

  .hero-promo-card__seal-main {
    font-size: 0.68rem;
  }

  .hero-promo-carousel__footer {
    position: relative;
    z-index: 5;
    justify-content: center;
  }

  .hero-mobile-brand {
    display: flex;
    width: 100%;
    flex-direction: column;
    align-items: center;
    gap: 0.45rem;
    animation: fadeUp 0.6s ease both;
  }

  .hero-mobile-brand__icon {
    width: 2.35rem;
    height: 2.35rem;
    object-fit: contain;
  }

  .hero-mobile-brand__wordmark {
    display: block;
    height: 1.4rem;
    width: auto;
    max-width: 10.5rem;
    object-fit: contain;
    mix-blend-mode: multiply;
  }

  .hero-content__intro {
    display: flex;
    width: 100%;
    max-width: 22rem;
    flex-direction: column;
    align-items: center;
    text-align: center;
    animation: fadeUp 0.72s 0.06s ease both;
  }

  .hero-eyebrow {
    justify-content: center;
    margin-bottom: 0.55rem;
    color: var(--gold-dark);
    font-size: 0.56rem;
    letter-spacing: 0.32em;
  }

  .hero-eyebrow-line {
    background: var(--gold);
  }

  .hero-title {
    margin: 0;
    width: 100%;
    max-width: 11.5em;
    font-size: clamp(2rem, 9.5vw, 2.75rem);
    line-height: 1.04;
    letter-spacing: -0.01em;
    text-align: center;
    color: var(--charcoal);
    text-shadow: 0 2px 16px rgba(248, 244, 239, 0.9);
    text-wrap: balance;
  }

  .hero-title em {
    color: var(--gold);
  }

  .hero-body {
    position: relative;
    z-index: 2;
    margin-top: 1.1rem;
    padding: 1.35rem 1.15rem 1.2rem;
    border-radius: 1.25rem;
    background: var(--white);
    border: 1px solid rgba(184, 150, 110, 0.16);
    box-shadow: var(--shadow-card);
    animation: fadeUp 0.8s 0.12s ease both;
  }

  .hero-lead {
    max-width: none;
    margin-bottom: 1.1rem;
    font-size: 0.84rem;
    line-height: 1.75;
    color: var(--taupe);
    text-align: center;
  }

  .hero-actions {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.6rem;
  }

  .hero-actions .btn {
    width: 100%;
    min-width: 0;
    padding: 0.85rem 0.65rem;
    font-size: 0.6rem;
    letter-spacing: 0.13em;
  }

  .hero-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.45rem;
    margin-top: 1.1rem;
    padding-top: 1rem;
    border-top: 1px solid var(--sand-dark);
    text-align: center;
  }

  .hero-stat-value {
    font-size: 1.1rem;
  }

  .hero-stat-label {
    font-size: 0.48rem;
    letter-spacing: 0.06em;
    line-height: 1.25;
  }

  .hero-scroll {
    display: none;
  }
}

@media (max-width: 380px) {
  .hero-actions {
    grid-template-columns: 1fr;
  }

  .hero-title {
    font-size: clamp(1.95rem, 10vw, 2.55rem);
  }

  .hero-stat-label {
    font-size: 0.44rem;
  }
}

/* â”€â”€â”€ Responsive â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
@media (max-width: 900px) {
  .product-detail-grid { grid-template-columns: 1fr !important; gap: 2rem !important; }
}

/* â”€â”€â”€ Home categories â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.home-categories {
  padding: clamp(4rem, 7vw, 5.5rem) 0;
  background: var(--white);
}

.home-categories__header {
  max-width: 34rem;
  margin: 0 auto clamp(2.5rem, 5vw, 3.5rem);
  text-align: center;
}

.home-categories__eyebrow {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.75rem;
  margin: 0 0 0.85rem;
  font-family: var(--font-sans);
  font-size: 0.62rem;
  font-weight: 600;
  letter-spacing: 0.28em;
  text-transform: uppercase;
  color: var(--gold);
}

.home-categories__eyebrow-line {
  display: inline-block;
  width: 28px;
  height: 1px;
  background: var(--gold);
}

.home-categories__title {
  margin: 0;
  font-family: var(--font-serif);
  font-size: clamp(2rem, 4.5vw, 3rem);
  font-weight: 300;
  line-height: 1.12;
  letter-spacing: -0.01em;
  color: var(--charcoal);
  text-wrap: balance;
}

.home-categories__grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 0.85rem;
}

.home-categories__card {
  position: relative;
  display: block;
  min-height: clamp(210px, 34vw, 280px);
  overflow: hidden;
  border-radius: 1.15rem;
  border: 1px solid rgba(184, 150, 110, 0.14);
  text-decoration: none;
  box-shadow: 0 16px 40px -28px rgba(92, 74, 58, 0.35);
  transition:
    transform 0.35s cubic-bezier(0.22, 1, 0.36, 1),
    box-shadow 0.35s ease,
    border-color 0.35s ease;
}

.home-categories__card:hover {
  border-color: rgba(184, 150, 110, 0.42);
  box-shadow: 0 22px 48px -24px rgba(92, 74, 58, 0.42);
  transform: translateY(-3px);
}

.home-categories__card--jewellery {
  grid-row: span 2;
  min-height: 100%;
}

.home-categories__card--newArrivals {
  grid-column: 1 / -1;
  min-height: clamp(180px, 28vw, 240px);
}

.home-categories__media {
  position: absolute;
  inset: 0;
}

.home-categories__img {
  object-fit: cover;
  object-position: center;
  transition: transform 0.55s cubic-bezier(0.22, 1, 0.36, 1);
}

.home-categories__card:hover .home-categories__img {
  transform: scale(1.05);
}

.home-categories__shade {
  position: absolute;
  inset: 0;
  background:
    linear-gradient(0deg, rgba(20, 14, 12, 0.78) 0%, rgba(20, 14, 12, 0.28) 42%, rgba(20, 14, 12, 0.08) 100%),
    linear-gradient(90deg, rgba(20, 14, 12, 0.35) 0%, transparent 55%);
}

.home-categories__card--newArrivals .home-categories__shade {
  background:
    linear-gradient(90deg, rgba(20, 14, 12, 0.82) 0%, rgba(20, 14, 12, 0.42) 38%, transparent 72%),
    linear-gradient(0deg, rgba(20, 14, 12, 0.45) 0%, transparent 60%);
}

.home-categories__content {
  position: absolute;
  inset: auto 0 0 0;
  z-index: 1;
  display: flex;
  flex-direction: column;
  gap: 0.3rem;
  padding: 1.15rem 1.1rem 1.2rem;
}

.home-categories__count {
  font-family: var(--font-sans);
  font-size: 0.56rem;
  font-weight: 600;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--gold-light);
}

.home-categories__name {
  margin: 0;
  font-family: var(--font-serif);
  font-size: clamp(1.15rem, 2.8vw, 1.45rem);
  font-weight: 400;
  line-height: 1.15;
  color: #fff;
}

.home-categories__arrow {
  margin-top: 0.15rem;
  font-family: var(--font-sans);
  font-size: 0.9rem;
  color: rgba(255, 255, 255, 0.72);
  transition: transform 0.28s ease, color 0.28s ease;
}

.home-categories__card:hover .home-categories__arrow {
  color: var(--gold-light);
  transform: translateX(4px);
}

@media (min-width: 768px) {
  .home-categories__grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
    grid-template-rows: repeat(2, minmax(240px, 1fr)) auto;
    gap: 1rem;
    align-items: stretch;
  }

  .home-categories__card {
    min-height: 0;
  }

  .home-categories__card--jewellery {
    grid-column: 1;
    grid-row: 1 / span 2;
    min-height: 100%;
  }

  .home-categories__card--homeDecor {
    grid-column: 2;
    grid-row: 1;
  }

  .home-categories__card--candles {
    grid-column: 3;
    grid-row: 1;
  }

  .home-categories__card--textiles {
    grid-column: 2;
    grid-row: 2;
  }

  .home-categories__card--gifts {
    grid-column: 3;
    grid-row: 2;
  }

  .home-categories__card--newArrivals {
    grid-column: 1 / -1;
    grid-row: 3;
    min-height: clamp(200px, 20vw, 240px);
  }

  .home-categories__card--newArrivals .home-categories__content {
    justify-content: center;
    inset: 0 auto 0 0;
    width: min(48%, 400px);
    padding: 2rem 2rem 2rem 1.75rem;
  }

  .home-categories__card--newArrivals .home-categories__name {
    font-size: clamp(1.5rem, 2.2vw, 1.85rem);
  }
}

@media (min-width: 1100px) {
  .home-categories__header {
    margin-bottom: 3.75rem;
  }

  .home-categories__grid {
    grid-template-rows: repeat(2, minmax(280px, 1fr)) auto;
    gap: 1.15rem;
  }

  .home-categories__card {
    border-radius: 1.35rem;
  }
}

/* â”€â”€â”€ Hover States (global, replaces style jsx) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */

/* â”€â”€â”€ Site footer (mobile-menu DNA) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  margin: -1px;
  padding: 0;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

.site-footer {
  position: relative;
  overflow: hidden;
  color: #f8f4ef;
  --footer-wave-h: clamp(2rem, 5vw, 3.25rem);
  --footer-bg-color: #140e0c;
  --footer-bg-glows:
    radial-gradient(ellipse 90% 55% at 10% -5%, rgba(184, 150, 110, 0.28) 0%, transparent 58%),
    radial-gradient(ellipse 70% 45% at 100% 85%, rgba(143, 180, 184, 0.16) 0%, transparent 52%),
    linear-gradient(168deg, #221a16 0%, #140e0c 42%, #0c0908 100%);
}

.site-footer__dark {
  position: relative;
  z-index: 2;
  margin-top: calc(-1 * var(--footer-wave-h));
  padding-top: var(--footer-wave-h);
}

.site-footer__dark::before {
  content: '';
  position: absolute;
  top: calc(-1 * var(--footer-wave-h));
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 0;
  pointer-events: none;
  background-color: var(--footer-bg-color);
  background-image: var(--footer-bg-glows);
  -webkit-mask-image:
    url("data:image/svg+xml,%3Csvg viewBox='0 0 1440 56' xmlns='http://www.w3.org/2000/svg' preserveAspectRatio='none'%3E%3Cpath fill='%23fff' d='M0 28C180 8 360 48 540 28S900 8 1080 28 1260 48 1440 28V56H0V28Z'/%3E%3C/svg%3E"),
    linear-gradient(#fff, #fff);
  mask-image:
    url("data:image/svg+xml,%3Csvg viewBox='0 0 1440 56' xmlns='http://www.w3.org/2000/svg' preserveAspectRatio='none'%3E%3Cpath fill='%23fff' d='M0 28C180 8 360 48 540 28S900 8 1080 28 1260 48 1440 28V56H0V28Z'/%3E%3C/svg%3E"),
    linear-gradient(#fff, #fff);
  -webkit-mask-size: 100% var(--footer-wave-h), 100% calc(100% - var(--footer-wave-h));
  mask-size: 100% var(--footer-wave-h), 100% calc(100% - var(--footer-wave-h));
  -webkit-mask-position: 0 0, 0 var(--footer-wave-h);
  mask-position: 0 0, 0 var(--footer-wave-h);
  -webkit-mask-repeat: no-repeat;
  mask-repeat